# Logo

`logo.png` — red background, white "HU" mark, transparent background.

This is the source of truth for `--fire` (`#AA0000`) — sampled directly
from this file's pixels, not chosen aesthetically. If the logo ever
changes, re-sample the red and update `tokens/tokens.css`.

Only one file here so far. Add exported variants as they exist (SVG,
different sizes, a monochrome/reversed version, favicon export, etc.).
